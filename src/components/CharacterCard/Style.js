import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    image: {
        resizeMode: 'cover'
    },
    row: {
        flexDirection: 'row',
        justifyContent: 'space-around'
    },
    infoText: {
        fontSize: 17,
        fontWeight: '600'
    }
});